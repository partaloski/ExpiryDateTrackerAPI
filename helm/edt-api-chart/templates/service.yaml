apiVersion: v1
kind: Service
metadata:
  name: {{ include "edt-api-chart.fullname" . }}
  labels:
    {{- include "edt-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edt-api-chart.selectorLabels" . | nindent 4 }}
